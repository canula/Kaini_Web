---
phase: 02-marketing-sections
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [index.html]
autonomous: false
requirements: [GLRY-01, GLRY-02, GLRY-03, GLRY-04]

must_haves:
  truths:
    - "Gallery shows 8 phone screenshots in a horizontal snap-scroll carousel on mobile"
    - "Desktop screenshot is displayed in the gallery section"
    - "Video plays inline with autoplay muted loop playsinline attributes"
    - "Phone screenshots are displayed flat with rounded corners and shadow — no CSS device frames"
    - "Gallery is responsive: carousel scrolls horizontally on mobile, adapts on desktop"
  artifacts:
    - path: "index.html"
      provides: "Gallery section with carousel, desktop screenshot, and video"
      contains: "id=\"gallery\".*snap-x.*snap-mandatory.*<video"
  key_links:
    - from: "Gallery carousel container"
      to: "Phone screenshot images"
      via: "CSS snap scroll with overflow-x-auto"
      pattern: "snap-x snap-mandatory"
    - from: "Video element"
      to: "Local video file"
      via: "HTML5 video with source elements"
      pattern: "<video.*autoplay.*muted.*loop.*playsinline"
---

<objective>
Build the gallery section with a CSS snap-scroll phone screenshot carousel, desktop screenshot display, and autoplay video embed — replacing the existing gallery stub in index.html.

Purpose: Show visitors TC-01 in action through real screenshots and video, building visual trust and demonstrating the app's interface before download.
Output: index.html with fully built gallery section; visual checkpoint for all Phase 2 sections.
</objective>

<execution_context>
@C:/Users/fudge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/fudge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-marketing-sections/02-CONTEXT.md
@.planning/phases/02-marketing-sections/02-RESEARCH.md
@.planning/phases/02-marketing-sections/02-01-SUMMARY.md

<interfaces>
<!-- Gallery stub to replace (current index.html lines 134-140): -->
```html
<section id="gallery" class="bg-surface py-24">
  <div class="max-w-6xl mx-auto px-6">
    <h2 class="text-3xl font-bold text-heading mb-4">Gallery</h2>
    <p class="text-body">Screenshots and video coming soon.</p>
  </div>
</section>
```

<!-- Asset paths (from RESEARCH.md convention): -->
Phone screenshots: assets/images/screenshots/phone-01.png through phone-08.png
Desktop screenshot: assets/images/screenshots/desktop-01.png
Video: assets/videos/demo.mp4 (and/or demo.webm)

<!-- Tailwind v4 CDN constraints (from RESEARCH.md): -->
- Use `bg-linear-to-*` NOT `bg-gradient-to-*`
- Scrollbar hiding: `[&::-webkit-scrollbar]:hidden [-ms-overflow-style:none] [scrollbar-width:none]`
- No `@utility` or `@apply` — use arbitrary property syntax
- Snap carousel: `overflow-x-auto snap-x snap-mandatory` on container, `snap-center shrink-0` on children

<!-- Existing page section order: hero → features → gallery → web-app → contact → footer -->
<!-- Gallery uses bg-surface (dark) to alternate with features (bg-surface-alt) above -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build gallery section with snap carousel, desktop screenshot, and video</name>
  <files>index.html</files>
  <action>
Replace the gallery stub section with a full gallery. Implementation details:

**Section structure:**
- Keep `id="gallery"` and `bg-surface py-24`
- Container: `max-w-6xl mx-auto px-6`
- h2: "See it in action" (text-3xl font-bold text-heading mb-3)
- Subtitle p: "TC-01 on Android — swipe through the screenshots." (text-body mb-10)

**Phone screenshot snap carousel:**
```html
<div class="flex overflow-x-auto snap-x snap-mandatory gap-4 pb-4
            [&::-webkit-scrollbar]:hidden [-ms-overflow-style:none] [scrollbar-width:none]">
```
- 8 screenshot items, each: `<div class="snap-center shrink-0 w-48 md:w-56">`
- Inside each: `<img src="assets/images/screenshots/phone-0N.png" alt="TC-01 screenshot N — [brief description]" class="rounded-2xl shadow-lg w-full" />`
- Use relative paths (no leading slash) for GitHub Pages compatibility
- Alt text should be descriptive for each screenshot (e.g., "TC-01 screenshot 1 — main melody generator screen")
- Screenshots displayed FLAT with rounded corners and shadow — NO CSS device frames (per user decision)

**Desktop screenshot:**
- Place below the carousel with `mt-12`
- `<img src="assets/images/screenshots/desktop-01.png" alt="TC-01 desktop interface" class="rounded-2xl shadow-lg w-full" />`

**Video embed:**
- Place below desktop screenshot with `mt-12 flex justify-center`
- Portrait aspect ratio (phone screen recording) — constrain width: `w-48 md:w-64`
- MUST include ALL FOUR attributes: `autoplay muted loop playsinline` (missing any one breaks mobile autoplay)
- Use poster attribute for loading state: `poster="assets/images/screenshots/phone-01.png"`
- Include both WebM and MP4 sources (WebM first for better compression):
```html
<video autoplay muted loop playsinline
       poster="assets/images/screenshots/phone-01.png"
       class="w-48 md:w-64 rounded-2xl shadow-lg">
  <source src="assets/videos/demo.webm" type="video/webm" />
  <source src="assets/videos/demo.mp4" type="video/mp4" />
</video>
```

**CRITICAL — avoid these anti-patterns:**
- Do NOT use `overflow-x-hidden` on carousel (prevents scrolling) — use `overflow-x-auto`
- Do NOT forget `snap-center shrink-0` on child items (without these, snap won't work)
- Do NOT use leading slashes in asset paths (`/assets/...`) — use relative paths (`assets/...`)
- Do NOT use `bg-gradient-to-*` — use `bg-linear-to-*` if any gradients needed
  </action>
  <verify>
    <automated>Open index.html in browser. Verify: (1) Gallery section shows phone screenshots in a horizontally scrollable row, (2) scrolling snaps to each screenshot, (3) desktop screenshot appears below carousel spanning full width, (4) video element visible below desktop screenshot with portrait aspect ratio, (5) resize to 375px — carousel is swipeable/scrollable, images have rounded corners and shadow.</automated>
  </verify>
  <done>Gallery section displays 8 phone screenshots in CSS snap-scroll carousel (flat with rounded corners, no device frames), desktop screenshot below, and autoplay muted looping video at bottom. Scrollbar is hidden. All asset paths use relative URLs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All Phase 2 marketing sections: hero with split layout and store badge CTAs, features grid with 6 capabilities and SVG icons, gallery with snap-scroll carousel and video, and contact placeholder.</what-built>
  <how-to-verify>
    1. Open index.html in a browser at desktop width (1280px+):
       - Hero: Split layout — headline + 3 CTAs on left, phone screenshot on right, gradient background
       - Features: 6 cards in 2x3 grid, each with orange SVG icon, title, and description
       - Gallery: Phone screenshots in scrollable row, desktop screenshot, video playing
       - Contact: "Get in touch" heading with empty form container
    2. Resize to mobile (375px):
       - Hero stacks vertically (text first, then phone mockup)
       - Features stack to single column
       - Gallery carousel is swipeable horizontally
       - Video plays inline (not fullscreen)
    3. Click "Launch Web App" CTA — smooth-scrolls to #web-app section
    4. Verify overall visual quality: typography, spacing, color consistency with dark theme
    5. Check that no sections have broken images (expected: phone screenshots and video may 404 if asset files not yet placed — that's OK, the HTML structure should be correct)
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 2, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
1. Open index.html — gallery section visible between features and web-app sections
2. Phone carousel: 8 image slots, horizontal scroll with snap behavior, hidden scrollbar
3. Desktop screenshot: full-width image below carousel
4. Video: portrait-oriented, has autoplay/muted/loop/playsinline attributes (inspect in DevTools)
5. Mobile (375px): carousel swipes, video plays inline
6. All Phase 2 sections present and responsive (hero, features, gallery, contact)
</verification>

<success_criteria>
- Gallery shows 8 phone screenshots in CSS snap-scroll carousel with rounded corners and shadow
- Desktop screenshot displayed below carousel
- Video element has autoplay, muted, loop, playsinline attributes and WebM+MP4 sources
- Scrollbar hidden on carousel using cross-browser arbitrary property classes
- Visual checkpoint confirms all Phase 2 sections look correct at desktop and mobile widths
</success_criteria>

<output>
After completion, create `.planning/phases/02-marketing-sections/02-02-SUMMARY.md`
</output>
