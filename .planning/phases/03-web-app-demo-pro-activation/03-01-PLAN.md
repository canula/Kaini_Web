---
phase: 03-web-app-demo-pro-activation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - APP-03

must_haves:
  truths:
    - "Page background is charcoal grey, not dark navy"
    - "Nav no longer has a Web App link; Pricing link exists in both desktop and mobile nav"
    - "The #web-app stub section is completely removed from the page"
    - "Six features are displayed in alternating two-column rows with paired images/video"
    - "The old Features grid and Gallery carousel sections are gone"
    - "Hero Launch Web App CTA points to a placeholder external URL with target=_blank"
  artifacts:
    - path: "index.html"
      provides: "Charcoal palette, updated nav, paired feature/image layout"
      contains: "--color-surface: #1a1a1a"
  key_links:
    - from: "index.html @theme block"
      to: "meta theme-color tag"
      via: "matching hex values"
      pattern: "theme-color.*#1a1a1a"
    - from: "index.html nav"
      to: "#pricing section"
      via: "anchor link"
      pattern: "href=\"#pricing\""
    - from: "index.html paired feature rows"
      to: "assets/images/screenshots/"
      via: "img src attributes"
      pattern: "assets/images/screenshots/phone-0[2367]\\.png"
---

<objective>
Restructure index.html: swap the dark navy palette to charcoal grey, update navigation (remove Web App, add Pricing), delete the #web-app stub section, and replace the separate Features grid + Gallery carousel with six paired feature/image alternating rows.

Purpose: This is the foundational restructure that changes page layout and theme before the Pro comparison section is added in Plan 02.
Output: Updated index.html with charcoal palette, clean nav, and paired feature/image sections.
</objective>

<execution_context>
@C:/Users/fudge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/fudge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-web-app-demo-pro-activation/03-CONTEXT.md
@.planning/phases/03-web-app-demo-pro-activation/03-RESEARCH.md
@index.html

<interfaces>
<!-- Current @theme block (lines 58-67 of index.html) — will be replaced -->
@theme {
  --color-accent: #f97316;
  --color-accent-hover: #ea580c;
  --color-surface: #0f172a;       <!-- CHANGE to #1a1a1a -->
  --color-surface-alt: #1e293b;   <!-- CHANGE to #2a2a2a -->
  --color-heading: #f8fafc;       <!-- KEEP -->
  --color-body: #94a3b8;          <!-- KEEP -->
  --color-nav: #0f172a;           <!-- CHANGE to #1a1a1a -->
}

<!-- Current nav links (desktop: line 85-88, mobile: line 107-110) -->
Features | Gallery | Web App | Contact
<!-- Target nav links -->
Features | Pricing | Contact

<!-- Current sections order -->
#hero | #features | #gallery | #web-app | #contact | footer
<!-- Target sections order -->
#hero | #features (paired rows) | #pricing (Plan 02) | #contact | footer
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Charcoal palette swap, nav updates, and #web-app removal</name>
  <files>index.html</files>
  <action>
Make three sets of changes to index.html:

**1. Palette swap — @theme block (around line 58-67):**
Replace the three navy values in the @theme block:
- `--color-surface: #0f172a` → `--color-surface: #1a1a1a`
- `--color-surface-alt: #1e293b` → `--color-surface-alt: #2a2a2a`
- `--color-nav: #0f172a` → `--color-nav: #1a1a1a`
Keep accent, accent-hover, heading, body unchanged.

Also update the meta theme-color tag (around line 26):
- `<meta name="theme-color" content="#0f172a" />` → `<meta name="theme-color" content="#1a1a1a" />`

**2. Navigation updates:**
Desktop nav (around line 84-89):
- Remove the `<a href="#gallery"...>Gallery</a>` link
- Remove the `<a href="#web-app"...>Web App</a>` link
- Add `<a href="#pricing" class="text-body hover:text-heading transition-colors">Pricing</a>` between Features and Contact

Mobile menu dropdown (around line 106-111):
- Remove the `<a href="#gallery"...>Gallery</a>` link
- Remove the `<a href="#web-app"...>Web App</a>` link
- Add `<a href="#pricing" class="block text-body hover:text-heading transition-colors">Pricing</a>` between Features and Contact

Hero CTA (around line 130):
- Change `<a href="#web-app" class="inline-flex...">Launch Web App</a>`
  to `<a href="https://PLACEHOLDER_APP_URL" target="_blank" rel="noopener noreferrer" class="inline-flex items-center gap-2 bg-accent hover:bg-accent-hover text-white font-semibold px-6 py-3 rounded-lg transition-colors">Launch Web App</a>`

**3. Remove #web-app section entirely:**
Delete the entire `<section id="web-app"...>...</section>` block (around lines 279-284).

After changes, search the file for any remaining `#web-app` references — there should be zero.
  </action>
  <verify>
    <automated>bash -c "grep -c '#web-app' C:/Users/fudge/Desktop/MyProjects/Sequencer-Landing/index.html | grep '^0$' && grep '#1a1a1a' C:/Users/fudge/Desktop/MyProjects/Sequencer-Landing/index.html | head -2 && grep '#pricing' C:/Users/fudge/Desktop/MyProjects/Sequencer-Landing/index.html | head -2"</automated>
  </verify>
  <done>Zero #web-app references remain. @theme has charcoal values #1a1a1a/#2a2a2a. Nav has Pricing link. Meta theme-color matches #1a1a1a. Hero CTA points to placeholder external URL with target=_blank.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Features grid and Gallery carousel with paired feature/image alternating rows</name>
  <files>index.html</files>
  <action>
Replace the entire `<section id="features">` and `<section id="gallery">` blocks with a single new `<section id="features">` that uses paired feature/image rows in alternating two-column layout.

Delete the existing Features section (lines ~152-227) and Gallery section (lines ~229-276).

Insert a new Features section with this structure:

```html
<section id="features" class="bg-surface-alt py-24">
  <div class="max-w-6xl mx-auto px-6">
    <h2 class="text-3xl font-bold text-heading mb-3">Features</h2>
    <p class="text-body mb-16">Everything you need to create melodies your way.</p>

    <!-- 6 paired feature/image rows, alternating layout -->
  </div>
</section>
```

**Row layout pattern:**
- Odd rows (1, 3, 5): text left, image right (default grid order)
- Even rows (2, 4, 6): text has `md:order-last` class, pushing image to left on desktop
- On mobile, all rows stack: text first, media below
- Each row: `<div class="grid md:grid-cols-2 gap-12 items-center py-16 border-b border-white/10">` (last row: no border-b)

**The 6 rows in order (from CONTEXT.md pairings + RESEARCH.md recommended order):**

Row 1 (text left, image right): **Record Voice**
- Reuse existing SVG icon (microphone — the path from current feature #1)
- Text: existing h3 + p from current Features section
- Media: `<img src="assets/images/screenshots/desktop-01.png" alt="TC-01 desktop interface showing voice recording" class="w-full rounded-2xl shadow-lg" />` (desktop screenshot = wide, use w-full)

Row 2 (image left, text right — text div gets `md:order-last`): **MIDI Import**
- Reuse existing SVG icon (download arrow — from current feature #2)
- Text: existing h3 + p
- Media: `<img src="assets/images/screenshots/phone-02.png" alt="TC-01 MIDI import screen" class="w-48 md:w-56 rounded-2xl shadow-lg" />`

Row 3 (text left, image right): **Motion / Contour / Rhythm Presets**
- Reuse existing SVG icon (sliders — from current feature #3)
- Text: existing h3 + p
- Media: video element with all 4 autoplay attributes:
  ```html
  <video autoplay muted loop playsinline
         poster="assets/images/screenshots/phone-01.png"
         class="w-48 md:w-64 rounded-2xl shadow-lg">
    <source src="assets/videos/demo.webm" type="video/webm" />
    <source src="assets/videos/demo.mp4" type="video/mp4" />
  </video>
  ```

Row 4 (image left, text right): **Phrase Generation**
- Reuse existing SVG icon (sparkles — from current feature #4)
- Text: existing h3 + p
- Media: `<img src="assets/images/screenshots/phone-06.png" alt="TC-01 phrase generation screen" class="w-48 md:w-56 rounded-2xl shadow-lg" />`

Row 5 (text left, image right): **Microtonal Support**
- Reuse existing SVG icon (musical note — from current feature #5)
- Text: existing h3 + p
- Media: `<img src="assets/images/screenshots/phone-03.png" alt="TC-01 microtonal tuning screen" class="w-48 md:w-56 rounded-2xl shadow-lg" />`

Row 6 (image left, text right — NO border-b on this last row): **Note Edit Tracker**
- Reuse existing SVG icon (pencil — from current feature #6)
- Text: existing h3 + p
- Media: `<img src="assets/images/screenshots/phone-07.png" alt="TC-01 note editor with shimmer effect" class="w-48 md:w-56 rounded-2xl shadow-lg" />`

**Each text column structure:**
```html
<div>
  <div class="flex items-center gap-3 mb-3">
    <svg class="w-8 h-8 text-accent flex-shrink-0" ...>[existing icon path]</svg>
    <h3 class="text-xl font-bold text-heading">Feature Name</h3>
  </div>
  <p class="text-body">Feature description.</p>
</div>
```

**Each media column structure:**
```html
<div class="flex justify-center">
  <img/video ... />
</div>
```

IMPORTANT: Use `bg-linear-to-br` NOT `bg-gradient-to-br` if any gradients are added (Tailwind v4 syntax). Asset paths must be relative (no leading slash) per project convention.
  </action>
  <verify>
    <automated>bash -c "grep -c 'md:order-last' C:/Users/fudge/Desktop/MyProjects/Sequencer-Landing/index.html | grep '^3$' && grep -c 'id=\"gallery\"' C:/Users/fudge/Desktop/MyProjects/Sequencer-Landing/index.html | grep '^0$' && grep 'autoplay muted loop playsinline' C:/Users/fudge/Desktop/MyProjects/Sequencer-Landing/index.html"</automated>
  </verify>
  <done>Old Features grid and Gallery carousel are gone. New #features section has 6 paired rows. 3 rows use md:order-last for alternating layout. Video has all 4 autoplay attributes. Images use correct screenshot assets per the pairing table. No #gallery section ID remains.</done>
</task>

</tasks>

<verification>
1. `grep '#web-app' index.html` returns nothing
2. `grep '#gallery' index.html` returns nothing (section removed)
3. `grep '#1a1a1a' index.html` finds @theme surface + nav + meta theme-color
4. `grep '#pricing' index.html` finds nav links (desktop + mobile)
5. `grep 'md:order-last' index.html` returns 3 matches (rows 2, 4, 6)
6. `grep 'autoplay muted loop playsinline' index.html` returns 1 match (video in row 3)
7. `grep 'PLACEHOLDER_APP_URL' index.html` returns 1 match (hero CTA)
8. Open index.html in browser — page is charcoal grey, features show alternating layout with images
</verification>

<success_criteria>
- Page uses charcoal grey palette (#1a1a1a / #2a2a2a) instead of navy
- Nav shows Features | Pricing | Contact (no Gallery or Web App)
- Hero CTA opens placeholder URL in new tab
- #web-app section is deleted
- 6 features displayed in alternating two-column paired rows with correct image/video assets
- Old Features grid and Gallery carousel are completely replaced
- Video element has all 4 mobile autoplay attributes
- All asset paths are relative (no leading slash)
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-app-demo-pro-activation/03-01-SUMMARY.md`
</output>
